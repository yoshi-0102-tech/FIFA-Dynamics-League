# 共通パスワード（閲覧・編集ともこのパスワードが必要）
APP_PASSWORD=change-me

# セッションCookie署名用の秘密鍵（ランダムな長い文字列。openssl rand -hex 32 などで生成）
AUTH_COOKIE_SECRET=change-me-too

# Supabase プロジェクト設定（Project Settings > API で確認）
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=change-me-service-role-key
